#!/usr/bin/env bash
# bin/use <build-dir>

PROCFILE=$1/Procfile

if [ -f ${PROCFILE} ]; then
  datomic=`sed '/^datomic/q' ${PROCFILE}`
  [ -n "$datomic" ] && exit 0 || exit 1
else
  echo "No Procfile or unrecognised" && exit 1
fi
